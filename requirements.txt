# Dependências principais
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Módulos futuros (descomente quando for implementar) ───────────────────────
# pyais>=4.3.0          # decodificador de mensagens AIS
# pyrtlsdr>=0.3.0       # receptor RTL-SDR (AIS via rádio VHF)
# pyserial>=3.5         # leitura de NMEA por porta serial
